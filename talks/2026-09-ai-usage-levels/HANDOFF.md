# HANDOFF — 2026-09-ai-usage-levels

## Stage

คิดหัวข้อ ✅ → outline ✅ → theme สี ✅ → ตัวละคร ✅ →
**สไลด์ 1–6 ทำแล้ว ✅** → **Agent Harness intro 5 beats ทำแล้ว ✅**
→ **Agent Harness maze 8 beats ทำแล้ว ✅** →
**Slide 09 Inbox-to-Ticket 70 beats ทำครบแล้ว ✅** → สรุป / หน้ารวม

## State

- **ชื่อ talk ล็อกแล้ว: "Level Up How You Use AI"** — เน้นว่าเป็นการยกระดับ
  วิธีใช้ของคน ไม่ใช่อัปเกรดตัว AI (folder slug ยังเป็น `ai-usage-levels` ตามเดิม)
- โครง 8 ช่วงล็อกแล้ว อยู่ใน NOTES.md — เกริ่น / ระดับการใช้ / ตัวอย่างงาน /
  chat / prompt / context / agent harness / สรุป
- Design หลัก: ช่วง 4–7 ใช้ตัวอย่างเดียวจากช่วง 3 เล่าต่อเนื่องเรื่องเดียวกัน
- วิธีทำ slides: HTML แยกไฟล์ต่อหน้า + HTML รวมที่เรียกผ่าน URL (**หน้ารวมยังไม่ได้ทำ**)
- **theme สีล็อกแล้ว** อยู่ใน `slides/theme.css`
- หน้า swatch: https://claude.ai/code/artifact/d8515ab2-a741-4ae7-b02e-ee6a0ef65aae
- **ตัวละครล็อกแล้ว** — user รับสไปรต์ชุดที่วาดใหม่รอบ 2 sprite ที่มี:
  `human` / `human-right` / `human-left` / `human-right-walk` / `human-left-walk` /
  `robot` / `robot-left` / `robot-right`
  **อย่าหันหัวเยอะกว่าท่า 3/4 ปัจจุบัน** เคยลองแล้วพังสองรอบ (profile เต็มตัวจนจำไม่ได้ /
  ตาสองข้างบนหัวที่หันเยอะจนออกมาเป็นสัตว์ประหลาด)
  **ไม่มีเฟรมเดินของหุ่น** — หุ่นเลย "ลอย" ไปมาแทนการเดิน (สไลด์ 1, 3, 4 ทำแบบเดียวกัน)

## ⚠️ อ่านตรงนี้ก่อนแตะสไลด์

**มีเอกสารประกอบสไลด์ครบแล้วที่ [`slides/docs/`](slides/docs/)** — เขียนไว้ให้
AI ตัวอื่นอ่านแล้วเข้าใจได้เร็ว **อ่านก่อนแก้สไลด์ทุกครั้ง**

| ไฟล์ | เนื้อหา |
| --- | --- |
| `slides/docs/README.md` | เรื่องที่ใช้ร่วมทุกสไลด์ — เวที 1280×720, ระบบ beat, ปุ่มควบคุม, iframe, ธีมสี, ตัวละคร, reduced-motion, วิธีเปิดดู |
| `slides/docs/01-title.md` | สไลด์ 1 เต็มๆ พร้อมกับดัก |
| `slides/docs/02-levels.md` | สไลด์ 2 เต็มๆ พร้อมกับดัก |
| `slides/docs/03-chat.md` | สไลด์ 3 เต็มๆ พร้อมกับดัก |
| `slides/docs/04-prompt.md` | สไลด์ 4 เต็มๆ พร้อมกับดัก |
| `slides/docs/05-context.md` | สไลด์ 5 เต็มๆ พร้อมกับดัก |
| `slides/docs/06-context-files.md` | สไลด์ 6 ทั้ง 9 beats, mock context docs, tree transitions และข้อควรระวัง |
| `slides/docs/07-agent.md` | Intro เข้า Agent Harness: ส่งพลัง สูบองค์ประกอบ และโคจรรอบหุ่น 5 beats |
| `slides/docs/08-agent.md` | เขาวงกต 8 beats + Growth Intelligence workflow, outputs และ guardrails |
| `slides/docs/09-inbox-to-ticket.md` | Inbox-to-Ticket workflow 6 phases / 70 beats + visual และ QA state |

## สไลด์ที่ทำเสร็จแล้ว

### `slides/01-title.html` — หน้าจอเปิดเกม GBC

2 beat: (1) กรอบ title เลื่อนลง → รอคลิก (2) คนเดินเข้าซ้าย + หุ่นลอยเข้าขวา
→ ทักทาย → หันเข้าหากันคุยกันเองไปเรื่อยๆ → รอคลิก → exit วูบมืด ไปสไลด์ 2
`▶ LET'S BEGIN` ทำหน้าที่เป็น "ไฟรอ input" โผล่ทุกครั้งที่จบ beat

### `slides/02-levels.html` — บันได 4 ระดับ

4 beat = 4 ระดับ เสาโผล่ทีละต้น + คนกระโดดขึ้นไปยืน เสาเก่าค้างไว้
เหนือบันไดมีแถบแสดงของกลางจอ เปลี่ยนตาม beat (แชท → prompt มีโครงสร้าง →
เอกสาร 2 ชั้น → เอกสาร 5 ชั้น + เครื่องมือ 6 ชิ้น) **ไม่มีหุ่นในสไลด์นี้โดยตั้งใจ**
ไอคอนเครื่องมือปั้นเป็น SVG พิกเซล 13×13 อยู่ในตัวแปร `ICONS` ในไฟล์สไลด์

### `slides/03-chat.html` — ระดับ 01 Chat เล่าด้วยบทสนทนาจริง

หน้าต่างแชทกลางจอ **1 คลิก = 1 ข้อความ** รวม 9 ข้อความ ฝั่งเราขวา (ครีมเหลือง)
ฝั่ง AI ซ้าย ทุกฟองมี avatar พิกเซลหน้าคน/หน้าหุ่นกำกับ กล่องสูงคงที่
ข้อความเก่าเลื่อนขึ้นหายไปเรื่อยๆ ไม่มี scrollbar
เคส IT support user EMP042 ล็อกอินเข้า StaffHub ไม่ได้ — AI เดา account ถูก lock
ก่อน แล้วสงสัยเรื่องสิทธิ์ พอเห็นรูป Access denied ก็ขอ login log เอง
พอได้ log ก็เจอว่าฝั่งสิทธิ์ยังอ้างอิงแผนก Sales อยู่ ทั้งที่ user ย้ายไป Finance
ตั้งแต่เมื่อวาน แล้วจบงานได้ ปิดท้ายด้วย `🙂👌`

**ใต้กล่องแชทมีฉากวนลูป** ไม่ผูกกับ beat เริ่มทันทีที่เข้าสไลด์ — หุ่นทำงานอยู่
ใน **ตู้ rack** ฝั่งซ้าย คนยืนรอฝั่งขวา คนเรียก → หุ่นออกมาคุย → หุ่นกลับเข้าตู้
ทำ **3 รอบ** รอบสุดท้ายคนขึ้น `✅` แล้ววนใหม่ (ตัวละคร 30% ของสไลด์ 1)

### `slides/04-prompt.html` — ระดับ 02 Prompt ยุบ 9 ข้อความเป็นข้อความเดียว

**6 beat** สองกล่องเทียบกัน: ซ้าย = บทสนทนาสไลด์ 3 ย่อเหลือแถวละบรรทัด 9 แถว /
ขวา = หน้าต่างแชทใหม่ที่ยังว่าง

1. เปิดมาเห็นทั้ง 9 แถว
2. **ซิป** — 4 แถวฝั่ง AI จางแล้วยุบสูงเป็น 0 กองรูดปิด หัวกล่อง 9 → 5
3. **FLIP** — 4 แถวที่เป็นข้อมูลจริงบินไปประกอบเป็น**ฟองแชทใบเดียว** พร้อมรูปหน้าจอ
   กับ log ที่แนบมาด้วย / 1 แถวที่ไม่ใช่ข้อมูล (sync แล้วเข้าได้) จางหายไปเฉยๆ
4. หน้าต่างแชทเลื่อนมากลางเวที + AI ขึ้นจุดสามจุด (= ส่งแล้ว)
5. คำตอบมาในฟองถัดไป root cause ตรงเป้าตั้งแต่คำตอบแรก
6. สรุป **9 → 2** + บรรทัดที่ชี้ว่าเรื่องย้ายแผนกคุณรู้ตั้งแต่แรก

ตัวนับหัวกล่องขวาไล่ 0 → 1 → 2 ข้อความ ขนานกับ 9 ของกล่องซ้าย

**ฉากล่างวนลูปเหมือนสไลด์ 3 แต่รอบเดียวจบ** (~22.5s) และ**บอลลูนฝั่งคน
"ค่อยๆ พิมพ์"** เป็น skeleton ย่อส่วนของฟองพรอมต์ (ย่อหน้า → บุลเล็ต 3 →
กล่องรูป → ย่อหน้าปิด) แต่ละแท่งกางจาก 0 บอลลูนขยายตาม หุ่นยืนรอตลอด
ฝั่งหุ่นยังโผล่ทั้งก้อนแบบเดิม — ตัดกันว่าคนใช้เวลาเรียบเรียง หุ่นตอบพรวดเดียว

### `slides/05-context.html` — ระดับ 03 Context ยกของที่ซ้ำไปไว้ในไฟล์เดียว

**6 beat** ซ้าย = ไฟล์ `context/staffhub-access.md` / ขวา = หน้าต่างแชท

1. พรอมต์ยาวใบเดิมจากสไลด์ 4 ผ่าสองกอง — `เฉพาะเคสนี้` / `เหมือนกันทุกเคส`
2. **พับ → บิน → คลี่** ไฟล์เปล่าโผล่มาก่อน แล้วย่อหน้าในกอง `keep` พับเป็นแถบบางๆ
   บินข้ามไปฝั่งซ้ายทีละอัน แล้วคลี่ออกเป็นบล็อกในไฟล์ (3 ย่อหน้า → 4 บล็อก ตาราง `FOLD`)
   **การจับคู่คือส่วนที่ให้ข้อมูล** ทำให้เห็นว่าไฟล์คือของเดิมที่ย้ายที่ ไม่ใช่ของใหม่
   ท่านี้จงใจไม่ให้ซ้ำสไลด์ 4 (นั่นคือของบินมาประกอบกัน อันนี้คือของถูกพับเก็บเข้าแฟ้ม)
3. ตั๋วใหม่ พิมพ์**ประโยคเดียว** ชิปชื่อไฟล์โผล่ใต้ข้อความ "แนบไปด้วยเสมอ"
4. ได้ root cause ตั้งแต่คำตอบแรก + **ไฮไลต์บรรทัดในไฟล์ที่หยิบไปใช้**
5. ตั๋วใบที่สอง ทีม Marketing เข้าแล้วค้าง ไฟล์เดิมไม่ได้แก้ **แต่ไฮไลต์ย้ายไปคนละบรรทัด**
6. หน้าต่างแชทเดิมยังอยู่ แต่ reset เนื้อหาแล้วทยอยขึ้น user-side message ของ
   **แชทใหม่คนละเคส** (`เคส 3` EMP118 / `เคส 4` EMP305 / `เคส 5` ทีม HR)
   พร้อม `···`; cue `แนบทุกครั้ง` ที่หัวไฟล์กะพริบหนึ่งครั้ง และล้าง highlight ของเคส 2
   ก่อนแสดงเคสใหม่ + บรรทัดกันโกง

**ฉากล่างมีตู้เก็บของ `.store` เพิ่มมาข้างตู้ rack** พอหุ่นเข้าไปทำงาน ช่องในตู้จะไล่ไฟ
= หุ่นไปเปิดไฟล์อ่านเอง คนไม่ต้องยื่นให้ **ลูปสั้นกว่าสไลด์ 4 เกือบครึ่ง (~19.5s)**
เพราะคนพูดคำเดียวจบ ไม่ต้องค่อยๆ พิมพ์เหมือนสไลด์ 4 — ความสั้นนี่แหละคือใจความ
ทั้งฉากเลื่อนไปกิน x 302..978 (เดิม 342..938) เพราะมีตู้เก็บของเพิ่มมา

### ระบบที่ใช้ร่วมกันทุกสไลด์

- เวที 1280×720 ตายตัว scale ให้พอดีจอ
- ระบบ **beat** — คลิก/`Space`/`→` เดินหน้าทีละจังหวะ / `←` ถอย / `R` reset
- ปุ่มมุมล่างขวา **ไม่มีกรอบ** เป็นกลิฟจาง `opacity .28` ชัดตอน hover:
  `◀ ▶ pips ↺ AUTO` — สไลด์ 3 (9 beat) / สไลด์ 4 (6) / สไลด์ 5 (6) ใช้เลข `n/N` แทน pips
- เส้นพื้น `bottom: 28px` (สไลด์ 1–2) / `30px` (ฉากสไลด์ 3) / `18px` (ฉากสไลด์ 4–5
  ที่เตี้ยกว่า 16px)
- รองรับโหมด iframe ด้วย `postMessage({type:"slide:next"|"slide:prev"})`

## เรื่องที่เคยคุยแล้วตกลงแล้ว (อย่ารื้อ)

- ของประกอบแต่ละ beat **แทนที่ ไม่สะสม**
- ข้อความในกล่อง/บอลลูนของ**ฉากตัวละคร** อ่านไม่ออกโดยตั้งใจ (แท่งขีด/สัญลักษณ์)
  เพื่อไม่ให้คนดูมัวอ่านแทนที่จะฟัง — ส่วนเนื้อสไลด์หลัก (สไลด์ 3–5) อ่านออกจริง
- beat ละ micro loop เดียว (beat 4 ของสไลด์ 2 มีสอง)
- **ไอคอน / องค์ประกอบ UI** ใช้ SVG ปั้นเอง **ห้าม emoji** (บังคับสีไม่ได้
  แตกพาเลตต์) และไม่ใช้ Unicode glyph (หน้าตาเปลี่ยนตามฟอนต์เครื่องที่ฉาย)
- **ข้อยกเว้น (ตกลงกัน 2026-08-29): emoji ใช้ได้เฉพาะเป็นสีหน้า/อารมณ์ของตัวละคร**
  เช่น `🙂👌` `✅` ในสไลด์ 3–4 — user รับความเสี่ยงเรื่องสีกับฟอนต์แล้ว
  แต่ **ไอคอน UI ยังห้ามใช้ emoji เหมือนเดิม**
- กติกา `--accent` แดงจุดเดียวต่อสไลด์ — **ผ่อนปรนแล้ว** ใช้ตามหน้างานได้
  สไลด์ 1 ใช้ที่หมวกหุ่น / สไลด์ 2 ใช้ที่เลขระดับของเสาที่ยืนอยู่ /
  สไลด์ 4 ใช้ที่เลข `2` ในบรรทัดสรุป / สไลด์ 5 ใช้ที่กอง `keep` (เส้นซ้าย + ป้าย)
  ซึ่งต่อเนื่องไปเป็นเส้นซ้ายของแถบที่พับบินในบีต 2

## เส้นเรื่อง IT support — เรื่องที่ตกลงแล้ว (อย่ารื้อ)

> **use case ล็อกแล้ว (2026-08-30): user `EMP042` ล็อกอินเข้า StaffHub ไม่ได้**
> เพราะเพิ่งย้ายจากแผนก Sales ไป Finance เมื่อวาน แต่ฝั่งสิทธิ์ยังไม่ได้ sync ตาม
> เส้นเรื่องเดียวนี้ลากยาวตั้งแต่สไลด์ 3 ถึงสไลด์ agent harness

- **เปลี่ยนแผนจาก outline เดิม** สไลด์ 3 พูดเรื่อง chat เลย ไม่ใช่ "ยกตัวอย่างงาน"
  (= ช่วง 3 ถูกยุบรวมเข้ากับช่วง 4) ชื่อไฟล์เลยเป็น `03-chat.html`
- **สไลด์ 3 มี 9 ข้อความ** (เคยเป็น 17) — user ตีกลับเวอร์ชัน 17 ข้อความเพราะยาว
  และเข้าใจยากกว่า เคส StaffHub สั้นกว่าและทุกคนในห้องเคยเจอ
- **เคสที่เคยใช้แล้วทิ้งไปแล้ว อย่าเอากลับมา:** ลูกค้าสร้าง order ไม่ได้ →
  ORD123 ยืนยันไม่ผ่านเพราะวงเงินเครดิตเป็น null (ตีกลับ: วงเงินเครดิตเป็นภาษา
  B2B/ขายส่ง ขัดกับภาพ checkout ร้านออนไลน์) → ORD123 ไม่ผ่านเพราะรหัสไปรษณีย์
  ไม่มีในตารางโซนจัดส่ง (ตีกลับ: ยาวไป เข้าใจยากกว่า)
- **บทจบด้วยความสำเร็จ ไม่ใช่ทางตัน** — chat ไม่ได้ผิด แค่แพงเวลา
- **เล่าเป็นภาษาธุรกิจก่อน แล้วค่อยยกหลักฐานทางเทคนิคตามหลัง** ใช้ทั้งสไลด์ 3/4/5
  log · DB · โค้ด ยังอยู่ครบในฐานะ *หลักฐาน* ไม่ใช่ตัวเปิดเรื่อง
- **⚠️ เรื่อง "เมื่อวานเพิ่งย้ายแผนก" ต้องโผล่ที่ข้อความที่ 7 เท่านั้น**
  มันคือของที่คุณรู้ตั้งแต่ต้นแต่เพิ่งบอก ซึ่งเป็นใจความทั้งหมดของสไลด์ 4
  ถ้าย้ายไปข้อความแรก สไลด์ 4 จะไม่มีอะไรให้เล่า
- **AI ต้องดูฉลาดแต่ข้อมูลไม่พอ ไม่ใช่ดูโง่** คำเดาแรก (account ถูก lock /
  password หมดอายุ) คือสาเหตุอันดับหนึ่งของ "ล็อกอินไม่ได้" จริงๆ
- **ตัวเลขที่ใช้:** user `EMP042` · `authorize.js:64` · ตั๋วใบสองในสไลด์ 5 คือ
  ทีม Marketing เข้าแล้วค้าง (ผูกกับกลุ่มสิทธิ์ 214 กลุ่ม) · ตั๋วที่รออยู่ท้าย
  สไลด์ 5 คือ EMP118 / EMP305 / ทีม HR
- **ที่อยู่ในไฟล์ context เป็นโดเมนสมมติทั้งหมด** (`staffhub.example.com` /
  `pg-prod`) อย่าเอาของจริงของบริษัทใส่
- **บรรทัดสรุปใต้กล่องแชทของสไลด์ 3 ถูกตัดออกแล้ว** ผู้พูดพูดจำนวนรอบเอง

## สไลด์ 4 — เรื่องที่ตกลงแล้ว (อย่ารื้อ)

- **ปลายทางของ FLIP ต้องเป็นฟองแชท ไม่ใช่การ์ดแบบฟอร์ม** (ตัดสินใจ 2026-08-30)
  เวอร์ชันแรกทำเป็นการ์ดมีป้ายช่อง 4 ช่อง + เส้นประรอเติม user ตีกลับเพราะ
  **มันเป็นภาษาภาพของ "เทมเพลต/โครงสร้าง" ซึ่งเป็นของสไลด์ 5 (context engineer)**
  ระดับ 02 ต้องเป็นแค่ "คุณพิมพ์ข้อความเดียวให้ครบ" ป้ายกำกับเลยกลายเป็นคำนำหน้า
  บรรทัดในตัวข้อความเอง (`เช็คไปแล้วดังนี้ครับ`)
  → **อย่าเอาเส้นประ / ช่องกรอก / ป้ายหัวช่องกลับมา**
- **แอนิเมชันยุบเลือกแบบ "ซิป → FLIP" เป็นสองบีต** (หนึ่งบีตหนึ่งความคิด)
  แบบที่พิจารณาแล้วไม่เอา: กองแล้วอัดเป็นการ์ดใบเดียว (ระหว่างอัดอ่านอะไรไม่ได้เลย) /
  ตัวหนังสือไหลออกจากฟอง (ต้อง clone ทุก text node แพงและเปราะ)
- **บีต 6 ต้องชี้ว่าเรื่องย้ายแผนกคุณรู้ตั้งแต่แรก แต่เพิ่งบอกตอนข้อความที่ 7**
  นั่นคือสารทั้งหมดของสไลด์นี้ — ไม่ใช่ว่า AI เก่งขึ้น แต่คุณส่งของให้ครบตั้งแต่ทีเดียว
- **ฟองปลายทางต้องถูกวางเต็มใบตั้งแต่ต้น beat 3 (เนื้อในล่องหน)** ห้ามเปลี่ยนไปเป็น
  "ค่อยๆ ต่อชิ้นเข้าไป" ไม่งั้นชิ้นหลังๆ วัดตำแหน่งแล้วบินไปลงผิดที่

## สไลด์ 5 — เรื่องที่ตกลงแล้ว (อย่ารื้อ)

- **ไฟล์เดียวเท่านั้น** (user ตีกลับเวอร์ชันแรก 2026-08-30) เวอร์ชันแรกทำเป็น
  **ชั้นวางที่มีการ์ดหลายใบ + ซ็อกเก็ตต่อ DB/log + แผงชื่อเครื่องมือ
  (Claude Code / Codex / Cursor)** ตีกลับทั้งชุด ระดับ 03 ต้องเป็นแค่
  *"คุณเขียนไฟล์ไว้ใบเดียว แล้วมันถูกแนบไปทุกครั้ง"* เรื่อง DB / log / API
  ยังอยู่แต่อยู่**ในไฟล์ ในฐานะ "ที่อยู่ของของ"** ไม่ใช่ท่อที่ต่อจริง
  → **อย่าเอาชั้นวางหลายชิ้น / ซ็อกเก็ต / ชื่อเครื่องมือ กลับมา**
- **ประโยคที่พิมพ์ในบีต 3 ต้องเป็นข้อความแรกของสไลด์ 3 คำต่อคำ**
  `user EMP042 ล็อกอินเข้า StaffHub ไม่ได้ครับ` — ประโยคเดียวกันเป๊ะที่สไลด์ 3
  ต้องคุยกัน 9 ข้อความกว่าจะจบ มาคราวนี้ถูกตั้งแต่คำตอบแรก **มุกทั้งสไลด์อยู่ตรงนี้**
  ถ้าจะแก้ต้องแก้ `03-chat.html` พร้อมกัน
- **มุกรอง: ไฟล์เดียวกันเป๊ะ แต่สองใบใช้คนละบรรทัด** ใบ 1 ไฮไลต์ `DB` + `โค้ดที่ตรวจ`
  + ขั้นตอน 2,3 / ใบ 2 ไฮไลต์ `Log` + `API` + ขั้นตอน 1,2 — นี่คือสิ่งที่ทำให้
  "ไฟล์เดียวพอ" จับต้องได้ ไม่ใช่แค่คำพูด (ทั้งสองใบใช้ `ทีมที่เกี่ยวข้อง` ร่วมกัน)
- **ตั๋วใบที่สองต้องเป็นอาการที่ไม่มี error** (ทีม Marketing เข้าแล้วค้าง) ไม่งั้นมันจะไป
  ใช้บรรทัดชุดเดิมกับใบแรก แล้วมุก "คนละบรรทัด" ไม่เกิด
- **บรรทัดกันโกงบีต 6 ห้ามตัด** — *ไฟล์ไม่ได้เขียนตัวเอง ต้องมีคนเขียนและคอยอัปเดต
  และมันบอกได้แค่ว่าของอยู่ที่ไหน ใครจะไปหยิบมาให้ ยังเป็นอีกเรื่อง*
  ประโยคหลังคือสะพานไปสไลด์ agent harness
- **ที่อยู่ในไฟล์เป็นโดเมนสมมติทั้งหมด** (`staffhub.example.com` / `pg-prod`)
  อย่าเอาของจริงของบริษัทใส่
- **`setBeat()` ต้องเรียก `rebuild(n-1)` ก่อนรัน `STEP[n]` เสมอ** เจอบั๊กจริงตอนกดรัว
  200ms แล้วไฟล์หายทั้งอัน เพราะ timer ของบีตก่อนโดน `clearTimers()` ล้างก่อน
  ได้ทำงาน — อย่าถอดออก
- **บีต 2 ต้องเป็น "พับ → บิน → คลี่" ห้ามกลับไปเป็นจางหายแล้วไฟล์โผล่พร้อมเนื้อ**
  (ตัดสินใจ 2026-08-30) เวอร์ชันจางหายทำให้คนดูต้องเชื่อเอาเองว่าไฟล์คือของชิ้นเดียว
  กับพรอมต์ ตอนนี้เห็นชัดว่าย่อหน้าไหนไปเป็นบล็อกไหน (ตาราง `FOLD`)
- **`foldFly()` วัดพิกัดตอนถูกเรียก ไม่ใช่วัดล่วงหน้า** เพราะกล่องแชทเลื่อนอยู่ 0.55s
  ต้นทางเลยต้องวัดหลังเลื่อนจบ (เริ่มบินที่ 680ms) — ต่างจากสไลด์ 4 ที่ต้องวัด
  ปลายทางล่วงหน้าเพราะฟองที่นั่นโตขึ้นเรื่อยๆ ส่วนบล็อกในไฟล์กินที่อยู่แล้วตั้งแต่แรก

## สไลด์ 6 — Context ที่ประกอบจากหลายไฟล์ (ทำครบ 9 beats แล้ว 2026-09-06)

ไฟล์งานคือ `slides/06-context-files.html` และเอกสารคู่กันคือ
`slides/docs/06-context-files.md`; `slides/05-context.html` ชี้ NEXT มาหน้านี้แล้ว

- Beat 1 เริ่มจาก `context/staffhub-access.md` ใบเต็มต่อเนื่องจากสไลด์ 5
- Beats 2–5 คง root ให้อ่านได้ทางซ้าย, highlight section ต้นทาง และดึงเอกสาร
  `logs.md`, `database.md`, `permissions.md`, `response-format.md` ไปทางขวาทีละใบ
- Context ย่อยใช้ข้อมูล mock แบบละเอียด เช่น server/IP, read replica/schema/SQL,
  roles/scope/sync batch, command และ response format; ห้ามแทนด้วยข้อมูล production จริง
- Beat 6 ซ้อนเอกสารย่อยทั้งสี่ใบ; Beat 7 ยุบกลับเข้า root โดยคงรูปแบบ section เดิม
  และแทนรายละเอียดด้วย cross-context refs
- Beat 8 ย่อ root เป็น thumbnail ทางซ้ายแล้วกาง tree ไปยังเอกสารลูกสี่ใบทางขวา
- Beat 9 ย่อ StaffHub tree ทั้งชุดเป็นช่องแรก แล้ว mini document trees รูปแบบเดียวกัน
  อีก 11 ชุดค่อย ๆ ปรากฏเป็นกริด 4×3; แต่ละชุดมี label อ่านได้และลึก 2–4 ชั้น
  โดยไม่มีเส้นเชื่อมข้ามคนละเรื่อง
- ตัดข้อความอธิบายว่าไฟล์ไม่เปิดหรือรัน tool เองออกจากสไลด์ตามคำขอของ user;
  user จะพูดประเด็นนี้เอง
- Back ไป `05-context.html`; NEXT ยังชี้ `07-agent.html`
- ภาษาภาพของสไลด์นี้คือการ zoom, แยก section และประกอบเป็น tree
  **ห้ามกลับไปใช้พับ → บิน → คลี่แบบสไลด์ 5**
- Beat 9 ล่าสุดผ่าน JavaScript syntax, `git diff --check`, browser flow Beat 8 ↔ 9,
  label/overflow checks และตรวจภาพที่ 1280×720 แล้ว
- แท็บ preview ถูกคืนไว้ที่ Beat 9; HTTP server ใช้ `python -m http.server 8777`
- user เดินหน้ามาทำสไลด์ 7 แล้ว จึงไม่มี implementation ค้างในสไลด์ 6

## สไลด์ Agent Harness ล่าสุด

- **`slides/07-agent.html` เป็น intro 5 beats แล้ว**: กลับมาใช้พื้น กรอบ title,
  เส้นพื้น, pips และ animation แบบ `01-title.html`; คนส่งก้อนพลัง Harness ให้หุ่น
  หุ่นชาร์จและสูบ Context, Tool, Decision, Human และ Verify เข้ามา จากนั้น Emoji
  ขนาดเล็กไม่มีกรอบ/label หมุนตามวงรีจริงรอบหุ่น หางแสงคำนวณจากตำแหน่ง Emoji
  ทุกเฟรมจึงไม่หลุดจากกัน และใช้ glow เบลอแทนเส้นประ โดยไม่บัง sprite
  user ตอบ “ok” หลังรอบแก้ glow/วงรีล่าสุด
- **`slides/08-agent.html` เป็นเขาวงกตเต็มจอ 8 beats** เปรียบเทียบ
  หุ่นตัวเดิมก่อนและหลังมี Harness; feedback รอบแรกถูกนำไปปรับแล้ว: กำแพงบาง
  สีเทา, เขาวงกต 12×6, ตัวละคร/หีบใหญ่ขึ้น, เส้นทางทองมี pulse, ป้าย Emoji+label
  ตกจากด้านบนเฉพาะ Beat 6 และค้างใน Beat 7–8, balloon มีรูปหีบ และหีบมี
  animation ตอนเปิด; user ตอบ “ok” หลังรอบแก้ล่าสุด
- **หน้ารวมสไลด์ยังไม่ได้ทำ** (โค้ดฝั่งสไลด์รองรับ postMessage ไว้แล้ว)
- Narrative และ visual ของสไลด์ agent harness ยังต้องคุยต่อ แต่ use case หลัก
  เปลี่ยนเป็น Growth Intelligence Harness แล้ว ตาม `slides/docs/08-agent.md`
- ตัวละครยังไม่มีสีหน้า/อารมณ์อื่น มีแค่หน้าปกติ

### Verification ล่าสุดของสไลด์ 07

- inline JavaScript syntax และ `git diff --check` ผ่าน
- เปิดด้วย Edge headless ที่ 1280×720 แล้วครบทั้ง 5 beats
- ตรวจช่วงสูบและวงโคจรจริงแล้ว ปลาย trail กับกึ่งกลาง Emoji คลาดกันต่ำกว่า 1px
- forward ไป Beat 5, back ไป Beat 4 และ reset กลับ Beat 1 ทำงาน
- navigation `06-context-files.html → 07-agent.html → 08-agent.html`
  และย้อนกลับทำงาน
- local preview server ใช้ `python -m http.server 8777`

**คำถามที่ user ยังไม่ตอบ:** จะทำให้ปุ่มควบคุมหายไปเองเมื่อไม่ขยับเมาส์ 2 วิไหม
(เพิ่มได้ทุกสไลด์ แต่ยังไม่ใช่งานรอบปัจจุบัน)

## สไลด์ 09 — Inbox-to-Ticket Harness (70 beats, user approved 2026-09-12)

ไฟล์งานคือ `slides/09-inbox-to-ticket.html` และเอกสารคู่กันคือ
`slides/docs/09-inbox-to-ticket.md`

- Workflow แสดงชื่อ 6 ด่านเป็นภาษาไทย: `รับเรื่อง` → `แยกประเด็น` → `ตรวจสอบ` →
  `สรุปผล` → `ระบุผู้รับผิดชอบ` → `ส่งต่องาน`
- ทุก phase ใช้ animation grammar เดียวกับ INTAKE: เปิด Markdown เต็มใบทีละใบ,
  ใบก่อนหน้าค้างเป็นกอง, รวมเป็น working set, ยุบเข้า checkpoint แล้วแสดง tree
  `Emoji + filename.md` ใต้ phase
- tree ของ phase ที่เสร็จแล้วต้องคงอยู่ใน horizontal world; กล้องเลื่อนไปหา phase
  ปัจจุบัน และ final overview ต้องเห็นครบทั้งหก tree
- ระหว่างอ่าน Markdown ใช้ Focus mode: workflow ด้านหลังเหลือ opacity 16%,
  active card ชัดเต็ม, กองเก่าจางลง และคืน workflow เป็น 100% ตอน collapse;
  INVESTIGATE lane โฟกัส Business / Technical พร้อมกันสองใบ
- ด่านรับเรื่องแสดงหัว `📥 รับเรื่อง` และ subtitle `อ่านและจัดรูปข้อมูล`
  ใน Beats 2–9 โดยวางหัว การ์ด และ working-set label ที่แนวเดียวกับด่านอื่น
  แล้วซ่อนหัวเมื่อยุบเข้า checkpoint ที่ Beat 10
- `INVESTIGATE` fan-out Business / Technical และมี technical Markdown แยกจริง
  สำหรับ structure, web, API, database, logs และ code
- Beat map: 1–10 รับเรื่อง, 11–20 แยกประเด็น, 21–33 ตรวจสอบ, 34–43 สรุปผล,
  44–54 ระบุผู้รับผิดชอบ, 55–68 ส่งต่องาน, 69 overview, 70 finish
- captions ทุก beatเป็นภาษาไทย; title ใน body ของทุกการ์ดมี Emoji
- `PREV` ของสไลด์ 09 ชี้ `08-agent.html`; `NEXT` ยังว่าง;
  `08-agent.html` เชื่อม NEXT มาหน้านี้แล้ว
- ผ่าน headless Chrome ครบ 70 beats ที่ 1280×720, back/reset/rapid navigation,
  tree milestones และ card overflow checks แล้ว
- user เปิดดู Focus mode เวอร์ชัน `?v=12` แล้วตอบว่า “เยี่ยม”; ถือว่า Slide 09
  ผ่านครบทั้งเนื้อหา animation และ visual hierarchy
- อย่ากลับไปใช้ card grid หรือ diagram สรุปสาม beatต่อ phase; ทุก phase ต้องค่อย ๆ
  สร้างกอง Markdown แบบเดียวกับ INTAKE

## Next action

1. พักงานไว้ก่อนตามคำขอเดิมของ user
2. เมื่อกลับมา ให้เริ่มจากคุยแนวคิดและภาพของ **สไลด์ปิด**; ยังไม่มีแบบที่ตกลงแล้ว
   จึงห้ามเดาแล้วลงมือทำทันที
3. หลังสไลด์ปิดผ่าน ค่อยเชื่อม `NEXT` จาก Slide 09 และพิจารณาหน้ารวม
4. ทดสอบ iframe navigation ทั้งชุดเมื่อสร้างหน้ารวมแล้ว
